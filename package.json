{
  "name": "deskhub",
  "version": "1.0.0",
  "description": "DeskHub - Support Ticket Dashboard",
  "main": "docs/src/main.js",
  "scripts": {
    "api": "json-server --watch db.json --port 3001",
    "ui": "live-server docs --port=8080 --open=index.html",
    "dev": "concurrently \"npm run api\" \"npm run ui\""
  },
  "dependencies": {
    "json-server": "^0.17.4"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "live-server": "^1.2.2"
  }
}
